package com.isaac.contactify.constant;

public class Constant {
    // The user.home it's going to reference the Users name, so it's a good way to make sure that it's going to work on anyone's PC
    public static final String PHOTO_DIRECTORY = System.getProperty("user.home") + "/Downloads/uploads/";
}
